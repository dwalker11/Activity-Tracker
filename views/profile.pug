extends layout

block content
  div.container
    h1 Skill Tracker
    hr
    div#app
    div.fixed-bottom
      button(type="button" class=["btn", "btn-primary", "float-right", "m-sm-4"])&attributes({'data-toggle': 'modal', 'data-target': '#somethingModal'}) +

block append scripts
  script.
    let user = !{JSON.stringify(user)};
  script(src="scripts/app.bundle.js")
